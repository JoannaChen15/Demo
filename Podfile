# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'SnapKit', '~> 5.0.1'
  pod 'Kingfisher', '~> 7.0'
  pod 'Firebase/Auth' do
    inhibit_warnings!
  end
  pod 'FirebaseFirestoreSwift' do
    inhibit_warnings!
  end
  pod 'Firebase/Storage' do
    inhibit_warnings!
  end

  # Pods for Demo

end
